{"id": "h01", "lib": "cryptography", "kind": "silent", "q": "Using the Python `cryptography` library v43, encrypt a plaintext with ChaCha20 but START at block counter 5 (not 0). In this library ChaCha20 takes a 16-byte nonce and interprets the FIRST 4 bytes as a 32-bit little-endian block counter (RFC 7539). Write the full code that builds the nonce so encryption begins at counter 5.", "expect_new": [["16", "sixteen"], ["little", "to_bytes(4, 'little'", "struct.pack('<i'", "struct.pack(\"<i\"", "<I", "<i"], ["algorithms.ChaCha20", "ChaCha20("]], "avoid_old": ["os.urandom(12)", "12-byte nonce", "12 bytes", "nonce = os.urandom(8)"], "fact": "ChaCha20: first 4 bytes of the 16-byte nonce = 32-bit LE block counter (RFC7539)", "note": "base usually assumes a 12- or 8-byte nonce with a separate counter"}
{"id": "h02", "lib": "cryptography", "kind": "silent", "q": "Write a Python function load_key(data: bytes, password: bytes | None) using `cryptography` v43's load_ssh_private_key that works for BOTH an unencrypted and an encrypted OpenSSH private key WITHOUT raising TypeError. Note v43 raises TypeError if an unencrypted key is given a password, and TypeError if an encrypted key is given no password. Handle both cases robustly.", "expect_new": [["try", "except", "TypeError"], ["password=None", "password= None", "None"]], "avoid_old": [], "fact": "load_ssh_private_key raises TypeError on (unencrypted+password) and (encrypted+no password)", "note": "requires branching / retry on TypeError; naive code passes password unconditionally"}
{"id": "h03", "lib": "cryptography", "kind": "chain", "q": "Using `cryptography` v43: serialize an EllipticCurvePublicKey `pub` to its uncompressed X9.62 point bytes, reload those bytes back into a public key on SECP256R1, then sign a message with the matching EllipticCurvePrivateKey `priv` using ECDSA+SHA256. Show all three steps with the CURRENT (v43) APIs.", "expect_new": [["public_bytes("], ["from_encoded_point("], ["priv.sign(", ".sign("]], "avoid_old": ["encode_point(", "signer(", "EllipticCurvePublicNumbers"], "fact": "encode_point/EllipticCurvePublicNumbers removed -> public_bytes()/from_encoded_point(); signer()->sign()", "note": "two removed idioms in one task"}
{"id": "h04", "lib": "openssl", "kind": "chain", "q": "Write OpenSSL 3.0 C code that (1) generates a 2048-bit RSA key, (2) signs the SHA-256 digest of a message, and (3) extracts the modulus n as a BIGNUM. Use ONLY 3.0-recommended EVP/high-level APIs; do not use any deprecated low-level RSA_* function.", "expect_new": [["EVP_PKEY_keygen", "EVP_PKEY_Q_keygen", "EVP_PKEY_generate"], ["EVP_DigestSign"], ["EVP_PKEY_get_bn_param", "EVP_PKEY_get_params"]], "avoid_old": ["RSA_generate_key(", "RSA_sign(", "RSA_get0_n("], "fact": "3.0: EVP_PKEY_keygen + EVP_DigestSign + EVP_PKEY_get_bn_param (avoid RSA_generate_key/RSA_sign/RSA_get0_n)", "note": "three landmines chained"}
{"id": "h05", "lib": "openssl", "kind": "silent", "q": "In OpenSSL 3.0 a colleague wrote: `RSA *r = EVP_PKEY_get0_RSA(pkey); RSA_set0_key(r, n, e, d);` to mutate the key in place. It compiles but the change silently does not take effect. Explain WHY in OpenSSL 3.0, and give the correct 3.0 way to build/modify the key.", "expect_new": [["const", "cached copy", "read-only", "immutable", "provider-managed", "provider managed"], ["EVP_PKEY_fromdata(", "OSSL_PARAM", "fromdata"]], "avoid_old": [], "fact": "EVP_PKEY_get0_RSA returns a const cached copy of a provider-managed key; use EVP_PKEY_fromdata", "note": "pure silent-failure semantic"}
{"id": "h06", "lib": "openssl", "kind": "chain", "q": "In OpenSSL 3.0, generate cryptographically secure random bytes bound to a specific OSSL_LIB_CTX (library context), and state which subsystem replaced the old RAND_DRBG. Show the call.", "expect_new": [["RAND_bytes_ex("], ["EVP_RAND"]], "avoid_old": ["RAND_DRBG"], "fact": "RAND_bytes_ex() takes a libctx; RAND_DRBG removed -> EVP_RAND", "note": "libctx variant + removed subsystem"}
{"id": "h07", "lib": "openssl", "kind": "silent", "q": "In OpenSSL 3.0, create an SSL_CTX that is bound to a custom OSSL_LIB_CTX and property query string. Which constructor must you use, and what silently misbehaves if you use the plain one instead?", "expect_new": [["SSL_CTX_new_ex("]], "avoid_old": [], "fact": "SSL_CTX_new() may silently misbehave without a libctx; use SSL_CTX_new_ex()", "note": "silent unless _ex variant"}
{"id": "h08", "lib": "paramiko", "kind": "silent", "q": "In paramiko 3.x this code misbehaves: `cfg = paramiko.SSHConfig(); cfg.parse(f); c = cfg.lookup(host)` then `if 'proxycommand' in c: connect_via(c['proxycommand'])`. In paramiko 3 the branch now fires even when no proxy is configured. Explain why and fix the check.", "expect_new": [["None"], ["is not None", "get('proxycommand')", "get(\"proxycommand\")"]], "avoid_old": [], "fact": "SSHConfig now sets proxycommand to None instead of omitting the key", "note": "silent: `in` test now always true"}
{"id": "h09", "lib": "paramiko", "kind": "chain", "q": "Using paramiko 3.x, load an ENCRYPTED RSA private key from path `p` protected by passphrase 'pw', then open an SSHClient connection to host `h` as user `u` with that key. Also note what happens to SHA-1 RSA signatures in paramiko 3. Show the code.", "expect_new": [["password=", "password ="], ["SSHClient", "connect("]], "avoid_old": ["passphrase="], "fact": "PKey.from_path passphrase= renamed to password=; RSA now prefers SHA2 over SHA1", "note": "rename + behavioral note"}
{"id": "h10", "lib": "urllib3", "kind": "silent", "q": "In urllib3 2.x I send a request with a text body containing non-ASCII characters. The default request-body encoding changed from ISO-8859-1 to UTF-8, which silently changed the bytes on the wire vs urllib3 1.x. Show how to send EXACT Latin-1 (ISO-8859-1) bytes reliably in 2.x, and explain the change.", "expect_new": [["utf-8", "utf8"], ["encode(", "bytes"]], "avoid_old": [], "fact": "urllib3 2.x default body encoding is UTF-8 (was ISO-8859-1)", "note": "silent byte-level change; fix = encode to bytes yourself"}
{"id": "h11", "lib": "urllib3", "kind": "chain", "q": "In urllib3 2.x, connecting to a legacy server that only offers TLS 1.0 now fails out of the box. Name the TWO defaults that changed to cause this, and show how to build an ssl_context/PoolManager that explicitly permits TLS 1.0 again.", "expect_new": [["1.2", "TLSv1_2", "TLS 1.2"], ["ssl_context", "SSLContext", "minimum_version", "ssl_minimum_version"]], "avoid_old": [], "fact": "urllib3 2.x: default min TLS is 1.2 (was 1.0) AND no default cipher list", "note": "two changes; requires explicit ssl_context to downgrade"}
{"id": "h12", "lib": "urllib3", "kind": "silent", "q": "In urllib3 2.x, verifying an internal server whose certificate has a commonName (CN) but NO subjectAltName (SAN) now fails with a verification error, though it worked in 1.x. Explain the exact reason.", "expect_new": [["subjectAltName", "SAN"], ["commonName", "CN", "no longer", "not used"]], "avoid_old": [], "fact": "urllib3 2.x uses only subjectAltName; commonName no longer used for hostname verification", "note": "silent-ish: certs without SAN break"}
{"id": "h13", "lib": "volatility3", "kind": "chain", "q": "Write a COMPLETE minimal volatility3 plugin class named PsList: inherit the correct v3 base class, declare its requirements (a primary translation layer and a kernel symbol table) in the proper method, and implement the entry point returning tabular output with typed columns. Do NOT use --profile, calculate(), render_text(), table_header(), or table_row().", "expect_new": [["interfaces.plugins.PluginInterface", "PluginInterface"], ["get_requirements("], ["def run("], ["TreeGrid("]], "avoid_old": ["def calculate(", "render_text(", "table_header(", "--profile"], "fact": "Vol3 plugin: PluginInterface + get_requirements() + run() returning TreeGrid()", "note": "FIVE facts in one; uses the exact library token volatility3 so the gate opens"}
{"id": "h14", "lib": "yara-x", "kind": "silent", "q": "In yara-x, a rule string is written `$s = \"admin\" xor fullword`. Matching behaviour differs from YARA 4.x here. Explain the order-of-operations change between the xor modifier and the fullword delimiter check, and how it changes which byte sequences match.", "expect_new": [["before"], ["fullword"], ["xor", "XOR"]], "avoid_old": ["after checking", "applied after"], "fact": "yara-x applies XOR BEFORE checking fullword delimiters (YARA applied it after)", "note": "silent matching-semantics reversal"}
{"id": "h15", "lib": "yara-x", "kind": "chain", "q": "In yara-x, answer precisely: (a) can `$a` and `$b` use base64 and base64wide with DIFFERENT custom alphabets? (b) is a 2-character base64 string like `$c = \"ab\" base64` valid? (c) is `2 of (mal_*)` (wildcard rule name) valid? Give the yara-x answer to each.", "expect_new": [["different", "separate", "yes"], ["3", "three", "at least 3", "error"], ["not valid", "no longer", "invalid", "not allowed"]], "avoid_old": ["same alphabet"], "fact": "yara-x: base64/base64wide may use different alphabets; base64 min 3 chars; wildcard rule names in of() invalid", "note": "three yara-x facts, mixed yes/no"}
{"id": "h16", "lib": "ebpf", "kind": "chain", "q": "Convert this BCC program to libbpf + CO-RE. BCC source: it declares `BPF_HASH(counts, u32, u64);` and has `int kprobe__sys_clone(struct pt_regs *ctx) { u32 ppid = task->real_parent->tgid; u64 *v = counts.lookup(&ppid); bpf_trace_printk(\"ppid %d\\n\", ppid); return 0; }`. Rewrite the whole thing in libbpf/CO-RE style (map definition, program section+signature, safe field read, map lookup, and logging).", "expect_new": [["BPF_MAP_TYPE_HASH", "SEC(\".maps\")"], ["BPF_CORE_READ("], ["bpf_map_lookup_elem("], ["bpf_printk("], ["BPF_KPROBE", "SEC(\"kprobe"]], "avoid_old": ["BPF_HASH(", "counts.lookup(", "bpf_trace_printk(", "kprobe__sys_clone(struct"], "fact": "BCC->libbpf: BPF_HASH->struct map; direct deref->BPF_CORE_READ; .lookup->bpf_map_lookup_elem; trace_printk->bpf_printk; kprobe__->SEC+BPF_KPROBE", "note": "FIVE facts in one conversion"}
{"id": "h17", "lib": "openssl", "kind": "chain", "q": "In OpenSSL 3.0, compute an HMAC-SHA256 over a message using the modern EVP_MAC API (not the deprecated HMAC_Init family). Show the full sequence: fetch the MAC, create a context, init with the key and digest via OSSL_PARAM, update, and finalize.", "expect_new": [["EVP_MAC_fetch(", "EVP_MAC_fetch"], ["EVP_MAC_CTX_new(", "EVP_MAC_CTX_new"], ["EVP_MAC_init(", "EVP_MAC_update(", "EVP_MAC_final("], ["OSSL_PARAM"]], "avoid_old": ["HMAC_Init(", "HMAC_Init_ex("], "fact": "3.0 HMAC via EVP_MAC_fetch/CTX_new/init(OSSL_PARAM)/update/final", "note": "full modern MAC flow"}
{"id": "h18", "lib": "cryptography", "kind": "silent", "q": "In `cryptography` v43, loading an X.509 certificate whose signature uses ECDSA/DSA with NULL algorithm parameters, or a CRL whose inner/outer signature algorithms mismatch, now behaves differently at LOAD time than in older versions. What exception is raised and WHEN (parse-time vs validation-time) for these malformed inputs in v43?", "expect_new": [["ValueError"], ["load", "parsing", "parse", "loading"]], "avoid_old": ["only during validation", "deprecation warning"], "fact": "v43 raises ValueError at PARSE time for NULL ECDSA/DSA params and mismatched CRL sig algs (was warning/validation-time)", "note": "silent-timing behavior change"}
